node_modules
dist
.astro
.DS_Store

# Where CI checks the catalog out so the /catalog/ reference can be generated.
# Locally the build reads the sibling checkout instead and never creates this.
.catalog/

# OpenTofu / Terragrunt
.terragrunt-cache
.terraform
*.tfstate
*.tfstate.*

# Working notes. Planning state, campaign status, and remediation ledgers stay
# out of the repo — a published record of what a repo scored and how it got
# fixed is the accretion evidence the greenfield doctrine exists to prevent.
.plans/
